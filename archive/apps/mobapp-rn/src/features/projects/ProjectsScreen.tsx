/**
 * @deprecated Facio 0.1 Slice A — root is ContinueScreen.
 * Kept as a thin re-export so old imports do not break mid-migration (D6).
 */
export { ContinueScreen as ProjectsScreen } from '@/features/continue/ContinueScreen';
