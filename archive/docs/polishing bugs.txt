# Polishing bugs — triage (PO 2026-08-03)

Source dogfood notes. Status routed into Facio 0.1 slices. Canon: `docs/Facio 0.1/13-continuity.md`, `14-impl-plan.md`.

| # | Note | Status | Where |
|---|------|--------|-------|
| 1 | Burger size differs Session vs Continue — unify glass icon chips | **done** (hotfix) | `GlassIconButton` + Session header |
| 2 | Stepper: no step back, only forward | **done** (Slice D) | `StepperPlayer` Back/Next |
| 3 | After measure → auto AI modernize plan (background) | **deferred** — not 0.1 shell; revisit after E | Product open / later |
| 4 | “Last day” chip on 1-day Guide feels wrong | **done** (hotfix) | Focus Engine `isLastDay` (horizon > 1) |
| 5 | Guides search; archive → status tags; repeat carbonara | **deferred** — search later; archive/tags = PO; Repeat → F | F + later |
| 6 | Stepper state resets when leaving Session → Continue → back | **done** (Slice D) | same-day `blockRuntimeByActionId` |
| 7 | Recompose Session: title → day N/M → large Block → why lower | **done** (Slice D) | Session Stage layout |
| 8 | Rest / day-kind copy is fitness-flavored for all domains (e.g. drawing Guide rest: «без силовой нагрузки»; «Тренировка» on art days) | **→ Slice G** (copy) or create `#2` prompt pass — not D | `path.restEmpty`, `dayKind.*`, LLM day titles |
| 9 | Create: user didn’t see expandable plan; answered questions but never hit «Update path» | **done** E2a-iterate (accepted) | Qs first + bg #2; skip→plan+Qs; answers→refine; selection |
| 10 | Active: Manual remove ingredient → expected whole plan cascade; couldn’t find path back to plan dialog | **landed — awaiting PO dogfood** (E2c) | Manual «Пересобрать с ИИ» → AI Feed; Diff + Undo; progress preserve |
| 11 | Create clarify free-form: typed text misaligned in answer field | **done** E2b-iterate | `PlanFeedQuestions` / ClarifyChips TextInput |
| 12 | Edit affordance: not session-global — pencil+Edit on the specific checklist/block (top-right) | **done** E2b-iterate | `BlockEditButton` on checklist/stepper/counter |
| 13 | Checklist Manual drag: jerky anim; row flew up once; first row keeps outline after other drags | **done** E2b-iterate — sortables (`60c9f54`) | `ManualBlockEditor` + `react-native-sortables` |
| 14 | Skip «без ответов» when no Qs + plan ready / Guides reopen | **done** (+ reveal regress fix) | no auto-reveal on Path ready; skip only clarify_first or real Qs |
| 14 | Notes-only / Guides reopen still showed «Собрать путь без ответов» | **landed** (uncommitted planFeed polish) | hide empty skip; force reveal if path ready |

## External dogfood (2026-08-05) — triage

**#9 Create discoverability (E2a-iterate, before or thin ahead of E2c)**
- Symptom: plan collapsed / «Update path» invisible → user stuck after answering chips.
- Fix direction (tester): step the feed — questions first with skip «Build plan without answers»; when plan exists, keep roadmap **expanded** (no hide-by-default).

**#10 Manual ≠ cascade (E2c — landed awaiting dogfood)**
- Symptom: removed shopping item via Manual; expected recipe texts + later steps to update; looked for back-to-chat, didn’t find it.
- Fix: ManualEdit **«Пересобрать с ИИ»** saves local tools then opens Active AI Feed seeded with rebuild reason; Guide **Edit plan** → same feed; apply → Diff → Undo (repair merge preserves progress).

## Hotfix (1 + 4) — DoD

1. One shared size for menu/settings glass chips (Continue, drawer gear, Session ≡); Session uses `GlassIconButton` + Ionicons, not emoji ☰.
2. Focus reason `lastDay` only when `cycle.horizon_days > 1` and unlock is last day; 1-day Guides never show «Последний день».

## Explicitly NOT now

3 and 5 stay deferred. **#8** domain-neutral rest/day copy → G / prompts (PO noted on C accept 2026-08-03).
