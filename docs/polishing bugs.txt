# Polishing bugs — triage (PO 2026-08-03)

Source dogfood notes. Status routed into Facio 0.1 slices. Canon: `docs/Facio 0.1/13-continuity.md`, `14-impl-plan.md`.

| # | Note | Status | Where |
|---|------|--------|-------|
| 1 | Burger size differs Session vs Continue — unify glass icon chips | **done** (hotfix) | `GlassIconButton` + Session header |
| 2 | Stepper: no step back, only forward | **→ Slice D** | Session Full Block |
| 3 | After measure → auto AI modernize plan (background) | **deferred** — not 0.1 shell; revisit after E | Product open / later |
| 4 | “Last day” chip on 1-day Guide feels wrong | **done** (hotfix) | Focus Engine `isLastDay` (horizon > 1) |
| 5 | Guides search; archive → status tags; repeat carbonara | **deferred** — search later; archive/tags = PO; Repeat → F | F + later |
| 6 | Stepper state resets when leaving Session → Continue → back | **→ Slice D** (atom / same-day resume) | Session persist |
| 7 | Recompose Session: title → day N/M → large Block → why lower | **→ Slice D** | Session Stage layout |

## Hotfix (1 + 4) — DoD

1. One shared size for menu/settings glass chips (Continue, drawer gear, Session ≡); Session uses `GlassIconButton` + Ionicons, not emoji ☰.
2. Focus reason `lastDay` only when `cycle.horizon_days > 1` and unlock is last day; 1-day Guides never show «Последний день».

## Explicitly NOT in hotfix

2, 6, 7 wait for **Slice D**. 3 and 5 stay deferred until PO reopens.
