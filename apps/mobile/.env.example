# Simulator / emulator on the same machine as the API:
EXPO_PUBLIC_API_URL=http://localhost:8000

# Physical device on the same Wi-Fi — use the Mac LAN IP instead:
# EXPO_PUBLIC_API_URL=http://192.168.1.10:8000
